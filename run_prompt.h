#ifndef MYSHELL_RUN_PROMPT
#define MYSHELL_RUN_PROMPT

/* Launches interactive prompt where user can enter commands to execute.
 * Returns exit value of the last command that was executed or 0 if none were.
 * */
int
run_prompt();
#endif /* ifndef MYSHELL_RUN_PROMPT */
